button that loads json playlist from file

playlist format:
{
    [
        "title": "string",
        "url": "string",
        "startTime": "string",
        "endTime": "string",
    ]
}

playlist area shows list of song titles

button for user to join wait list

waitlist area shows names of users

